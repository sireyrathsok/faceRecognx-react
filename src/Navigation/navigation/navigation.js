import React from "react";

const Navigation = () => {
  return (
    <nav className=" flex float-right cursor-pointer underline hover:text-white  text-lg ">
      <button>Sign Out</button>
    </nav>
  );
};

export default Navigation;
